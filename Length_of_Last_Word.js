/**
 * @param {string} s
 * @return {number}
 */
var lengthOfLastWord = function(s) {
    s = s.trim();
    let words = s.split(' ');
    return words[words.length - 1].length;
    //here in this Problem i am gonna need to calculate the length of the last word in the sentence..
    let subs = s.substring();
    
};